<%= @room.name %>
<%= @room.capacity %>
<%= button_to "Delete this room", @room, method: :delete %>

<% @room.bookings.each do |booking| %>
  <p>Start: <%= booking.start %></p>
  <p>End: <%= booking.end %></p>
  <p>Purpose: <%= booking.purpose %></p>
  <p><%= link_to "Bookings", booking %></p>
<% end %>